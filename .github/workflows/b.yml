name: Pipeline Ventas

on:
  workflow_run:
    workflows: ["Pipeline Docker"]
    types:
      - completed

jobs:
  job2:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "This is Job 2 in Pipeline Ventas"
